version: "3.8"

services:
  buildwheel: 
    container_name: TurtleShell
    
    build:
      context: .
      dockerfile: "Dockerfile"
      
    volumes:
      - .:/app
      - /app/node_modules #Anonymous Volume
      - /app/temp-crfsuite
      - /app/dist
      - /app/.venv
      - /app/.pyodide-xbuildenv-0.27.3

    environment:
      - CHOKIDAR_USEPOLLING=true
      - PYODIDE_CROSS_BUILD_ENV_METADATA_URL=https://pyodide.github.io/pyodide/api/pyodide-cross-build-environments.json
